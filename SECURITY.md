# 安全与隐私

## 支持范围

本项目只支持经过授权、低频、可暂停并由人工处理验证的数据采集流程。不得使用本项目绕过验证码、身份核验、访问控制或平台规则。

## 严禁提交

- 密码、短信验证码、Cookie、Token、Webhook 或 SSH 私钥。
- 数据库、对象存储、远程桌面或代理服务凭据。
- 完整手机号、真实账号归属表、真实 Profile 目录。
- 带签名参数的生产页面链接、请求头和会话标识。
- 真实 JSONL、Excel、截图或可识别个人/门店生产任务的运行日志。

提交前运行：

```powershell
pnpm public:verify
```

## 报告安全问题

请使用 GitHub 的 **Report a vulnerability** 私密报告功能。不要在公开 Issue 中粘贴凭据、完整手机号、生产 URL、截图或原始日志。

报告应包含：受影响版本、最小复现步骤、影响范围和已经采取的隔离措施。维护者会先确认接收，再协调修复和披露时间。

## 账号或站点异常

验证码、403/418、登录失效和 Profile 污染属于生产运行风险，不应作为“安全漏洞绕过”处理。请暂停受影响 Browser Slot，保留 checkpoint，按照 `docs/handoff/07-风险事件与人工处理.md` 和 `docs/handoff/14-账号风控Profile与登录操作手册.md` 进入人工闭环。
