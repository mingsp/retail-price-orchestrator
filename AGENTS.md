# Codex 项目操作规则

本项目是商圈比价数据采集调度系统。目标是在授权范围内，通过多设备、多账号、独立 Profile/CDP 和人工介入，低频、可暂停、可恢复地取得完整门店原始数据。

## 最高规则

1. 严禁创建或调用子智能体。
2. 默认先只读诊断。用户未明确要求时，不启动 Chrome、不连接真实 CDP、不发商品请求、不写生产数据库。
3. 不绕过验证码、身份核验、登录限制和平台风控。
4. 密钥、密码、Webhook、Token、完整手机号和 Cookie 不得写入代码、文档、命令记录或交接包。
5. 不删除、移动或覆盖 Profile、checkpoint、原始 JSONL、截图和历史产物，除非用户明确指定目标。
6. 不复用风险 Profile 登录新账号；风险 Profile 只能隔离并保留审计。
7. CDP 端口不是账号身份。所有操作必须核对 `Worker + Browser Slot + Account + Profile + Store`。
8. 门店 ID 未确认一致前，不允许发商品请求。
9. 已存在有效 checkpoint 时必须续采，不得默认从零重采。
10. 不把 progress 中的 `100%` 或进程正常退出直接当成门店完整证明。
11. 处理 403、418、验证码、登录失效或商品不可见前，必须先读
    `docs/handoff/14-账号风控Profile与登录操作手册.md`；不得凭经验连续刷新或试号。
12. 更换账号时必须同时使用新的纯采集 Profile。旧账号、旧 Profile 和风险事件保留审计，
    不复制 Cookie、localStorage、UUID、页面参数或深页游标。
13. 新任务或上下文恢复必须使用 `docs/handoff/16-Codex提示词手册与模板.md` 的结构化提示词。
    “继续”“开始”“不要停”只能在当前资源绑定和断点已经被本轮证据确认后使用。

## 当前主链路

- 控制面：`apps/master`
- 执行面：`apps/worker`
- 操作台：`apps/dashboard`
- 原生采集器：`scripts/native-cdp-store-capture.mjs`
- 部署：`infra/`、`deploy/`
- 交接入口：`HANDOFF_START_HERE.md`
- 当前交接规则：`docs/handoff/`

工作区根目录和历史 `_tmp_*` 目录中的脚本只用于追溯，不是默认生产入口。

## 生产动作门禁

真实采集前必须确认：

- Worker 在线且系统时间、磁盘和网络正常。
- Browser Slot 对应正确账号、纯采集 Profile、CDP 和目标门店。
- 页面可见、未登录失效、未出现验证码、403、418 或商品不可见。
- 类目分配固定且与其他账号不重叠。
- 输出目录、checkpoint 和任务租约正确。
- 使用批准的低频参数。

遇到验证码、403、418、登录异常、门店不匹配或商品不可见：

1. 立即停止该 Slot 新请求。
2. 保留 checkpoint、页面状态和原始产物。
3. 生成风险事件并通知人工。
4. 人工处理后重新核对账号和门店。
5. 从有效 checkpoint 恢复。

403 不能直接等同于“账号彻底封禁”。必须先按操作手册区分目标门店限制、即时零售业务线
限制、商品接口限制、账号限制和设备/IP 聚集风险，再决定冷却、换号、换 Profile 或暂停 Worker。

## 数据验收

原始 JSONL 是可重建事实，SPU/SKU 表、Excel 和业务报表是投影。完成必须同时核对：

- 计划类目与有效独立类目标记。
- 原始 SPU、嵌套 SKU 和结构化 SKU。
- 门店唯一商品与类目-商品关系。
- 前端展示价覆盖率和来源字段。
- 商品名称未被修改。
- artifact 大小、SHA-256、版本和重复执行。

## 修改与验证

- 编辑前先说明范围。
- 手工编辑使用 `apply_patch`。
- 遵循现有 TypeScript/Node 模式，不做无关重构。
- 测试、类型检查、生产构建和现场验证必须分开报告。
- 历史报告只能证明当时环境，不能替代当前验证。
