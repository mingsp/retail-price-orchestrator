global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: retail-radar-master
    static_configs:
      - targets: ["master:17890"]
