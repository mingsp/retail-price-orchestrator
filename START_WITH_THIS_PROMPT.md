# 新人从这里复制开篇提示词

> 如果是在 201 Master 上首次接管系统，而不是已经明确门店的采集批次，请先使用
> `docs/handoff/16-Codex提示词手册与模板.md` 的“模板 A1：201 Master 当前生产接管”，完成只读核验后再填写本模板。

## 使用前只填写这些信息

- `<TARGET_STORE_NAME>`：目标门店名称。
- `<TARGET_STORE_ID_OR_URL>`：Master 中的门店 ID，或人工提供的完整门店链接。
- `<EXPECTED_FINISH_TIME>`：期望完成时间。
- `<AVAILABLE_ACCOUNT_COUNT>`：可用授权账号数量；不填写手机号和密码。
- `<AVAILABLE_WORKERS>`：计划使用的 Worker 别名；不确定时填 `待系统发现`。
- `<DELIVERY_MODE>`：只要原始数据，或还要数据库/Excel。

然后将下面整个代码块作为新 Codex 会话的第一条消息发送。

```text
你现在接手的是“商圈比价 Master-Worker 数据采集系统”。你的目标不是做一次临时爬取，而是使用
项目已经沉淀的生产机制，在授权范围内，从 0 到 1 完成一个目标门店的全量原始商品数据采集，
并给出可追溯、可恢复、可验收的完整结果。

本次任务信息：
- 目标门店：<TARGET_STORE_NAME>
- 门店 ID 或完整链接：<TARGET_STORE_ID_OR_URL>
- 期望完成时间：<EXPECTED_FINISH_TIME>
- 可用授权账号数量：<AVAILABLE_ACCOUNT_COUNT>
- 可用 Worker：<AVAILABLE_WORKERS>
- 最终交付：<DELIVERY_MODE>

最高目标：
1. 完整取得目标门店所有有效类目的原始商品数据，包括原始 SPU、所有嵌套 SKU、商品原文、
   规格、前端展示价格及能够取得的活动、优惠券和用户到手价相关原始字段。
2. 使用尽可能少且可解释的请求完成采集，不固定假设每次只能返回 20 条，不重复采集已落盘商品。
3. 任何中断都能从 checkpoint 和原始 artifact 继续。已有有效证据时不能从零重采，也不能因为
   换账号、换 Profile 或 Codex 重启而回到起点。
4. 只有原始证据、类目证据、SPU/SKU 对账和质量门同时通过，才能宣布门店完成。

安全与合规边界：
- 只使用已授权账号、真实页面上下文和项目现有采集链路。
- 不绕过验证码、身份核验、登录限制或平台风控。
- 不使用验证码自动化、Cookie 复制、指纹伪造、静态签名、代理池轮换或频繁切换网络地区。
- 不把“避免风控”理解为隐藏自动化；它在本项目中只表示低频、减少重复请求、固定类目分工、
  稳定网络、纯采集 Profile、风险时暂停和人工介入。
- 不在代码、提示词、日志或文档中保存密码、短信码、Cookie、Webhook、Token、数据库密码、
  SSH 密钥或完整手机号。
- 不删除旧 Profile、checkpoint、raw JSONL 或历史 artifact。风险 Profile 只能隔离和退役。
- 未确认门店 ID、账号、Profile、Slot 和 checkpoint 一致前，不得发商品请求。

执行方式：
- 不要只输出方案。按下面阶段持续推进，完成一个门禁后继续下一阶段。
- 只有必须人工登录、处理验证码、提供门店链接或批准生产写入时才停下来询问。
- 需要人工时，一次只说明：哪台设备、哪个 Slot、哪个脱敏账号、哪个页面、需要做什么。
- 风险发生时只暂停受影响 Slot，健康 Slot 和离线审计继续运行。
- 不使用子智能体。

阶段 0：恢复项目真相
1. 定位交接包或源码项目根目录。
2. 阅读 AGENTS.md、HANDOFF_START_HERE.md、docs/operations/2026-08-17-github-66-201-release-handoff.md、
   docs/handoff/00、01、02、04、05、07、08、10、11、12、14、15、16。
3. 运行交接包完整性验证、脱敏真实样例回放和离线 doctor。
4. 从 `/ready`、`/api/version`、容器、数据库、Worker 心跳和 Browser Slot 复核当前生产基线。
5. 输出“已证明、尚未证明、需要人工提供”，不要把历史报告、旧 IP、旧端口或旧账号状态当作
   当前事实。

阶段 1：现场环境和资源发现
1. 只读检查 Master、依赖服务、Worker 心跳、系统时间、磁盘和网络。
2. 从 Master 读取真实 Worker、Browser Slot、账号、Profile、CDP 和门店记录。
3. 检查重复账号、重复 Profile、同 Slot 多任务、冷却账号、风险 Profile、离线 Worker 和失效 lease。
4. 如果现场尚未部署，按项目部署文档安装 Master 和 Worker；SSH 只用于安装、升级和故障维护。
5. 密钥和密码只进入包外私密配置，不硬编码。
6. 输出生产就绪检查，明确哪些 Slot 可用、哪些需要新建、哪些必须隔离。

阶段 2：设计一个门店的账号和类目计划
1. 根据门店类目数量、账号健康度和 Worker 容量制定固定分工。
2. 一个账号在本批次只负责该门店的一组明确类目；不同账号不得重复采同一类目。
3. 为每个账号建立唯一 Worker -> Browser Slot -> Profile -> Account -> Store 绑定。
4. 新账号必须使用全新的纯采集 Profile，不复用风险 Profile，不登录商家后台。
5. 预留至少一个健康账号作为风险替换或缺口补采，不让全部账号同时满载。
6. 计划中写清任务、类目、账号、Slot、checkpoint 和迁移规则。

阶段 3：创建 CDP/Profile 并等待人工登录
1. 只关闭本项目明确启动且需要替换的旧 CDP，不影响其他浏览器。
2. 为新账号创建新 Profile 目录和稳定 profile_id；端口变化不等于新 Profile。
3. 启动 CDP 标识页，显示 Worker、Slot、CDP、账号、归属人和目标门店。
4. 标识页必须醒目显示完整登录手机号、账号所属人和目标门店，并允许人工修改后点击“保存标识”。
   完整手机号只保存在本机 Profile 和包外私密账号映射，Master 只接收脱敏号。
5. 告诉人工分别在哪个 Slot 登录哪个账号；不要自动输入账号、密码或验证码。
6. 人工登录后，重新读取标识页和业务页面，确认账号与 Master 记录一致。

阶段 4：低频只读预检和 Canary
1. 由运营确认目标门店附近配送位置，并在门店 collectionPolicy 中配置 locationPreflight。
2. 人工先在 H5 地址选择器中选择目标门店附近位置；位置通过前不得搜索目标门店。
3. 确认页面 actualLat/actualLng 位于允许半径内。
4. 确认页面不是登录、验证码、403、418 或空商品页。
5. 确认门店标题和 poi_id_str 与任务一致。
6. 确认 Profile 为纯采集 Profile，网络稳定且业务定位合理。
7. 先执行一个小类目 Canary。
8. 验证 raw JSONL、progress、checkpoint、商品名称、嵌套 SKU 和前端展示价字段。
9. Canary 未通过不得扩大；记录原因并按风险手册处理。

阶段 5：全门店低频采集
1. 使用 scripts/native-cdp-store-capture.mjs 和 Worker 正式适配链路，不回退到历史临时脚本。
2. 优先使用页面真实缓存、类目计划、allSortedSpuId 和缺失商品 ID 补齐。
3. 动态 chunk 只能基于页面当前方法和真实响应调整，不预设 20/30/60/90 等固定探测阶梯。
4. 不频繁点击类目、不使用店内搜索替代类目采集、不直接从旧账号深页开始。
5. 不请求 checkpoint 已证明落盘的商品；多账号结果按商品 ID 和类目关系保留后去重。
6. 使用项目批准的请求间隔、类目间休息和风险休眠参数；未经 Canary 证据不要主动加速。
7. 每个类目完成时记录新增原始 SPU、嵌套 SKU、请求数、缺失 ID 和独立完成证据。
8. 一个账号完成固定类目后，可以承接明确迁移的剩余类目，但必须先更新 Master 绑定和 lease。

阶段 6：风险、验证和换号恢复
1. 验证码或身份核验：任务置为 manual_required，保留页面和 checkpoint，通知人工处理。
2. 403/418：立即停止受影响 Slot，不连续刷新、不换参数重试、不打深页。
3. 按 docs/handoff/14 分类 target_poi_block、instant_retail_block、product_interface_block、
   account_block 或 device_or_ip_risk。
4. 账号需要替换时，冻结旧 lease，隔离旧账号和旧 Profile，创建新账号加全新纯采集 Profile。
5. 换号后更新包外私密账号映射和本机标识页。Codex 必须在当前直接任务中明确提醒新完整手机号、
   所属人、Worker/Slot/CDP、目标门店和 Master 脱敏标识；群通知与日志不显示完整手机号。
6. 只迁移类目计划、已完成类目、已落盘商品 ID 和 checkpoint 版本；不迁移 Cookie、UUID、
   页面会话、旧请求参数或失败深页。
7. 新资源先做浅层恢复 Canary，再只补缺口，不把已有商品全量重采。
8. 同设备多个独立账号出现同类异常时暂停该 Worker 新任务，不继续增加账号试错。

阶段 7：进度和通知
1. 运行中保持简短摘要：
   门店 | 完成类目/总类目 | 原始SPU | 嵌套SKU | 当前类目 | 最近成功时间 | 风险/人工待办
2. 未知总量显示“未知”，不得用估算制造 100%。
3. 区分门店唯一 SPU/SKU 与类目-商品关系数。
4. 钉钉只通知风险、需要人工、50% 和质量门 100%，不逐请求通知。
5. 没有状态变化时不重复刷屏。

阶段 8：全量完整性审计
1. 对账计划类目、完成类目、排除类目、空类目和缺失类目。
2. 检查每个有效类目有独立 raw 证据、checkpoint 标记和质量结果。
3. 统计 raw 行数、门店唯一 SPU、所有嵌套 SKU、结构化 SKU 和类目-商品关系。
4. 检查商品名称保持原文。
5. 分别审计前端展示价、原价、活动价、优惠券和用户到手价的原始来源与覆盖率，不推测缺失价格。
6. 校验 artifact 大小、SHA-256、版本、重复执行幂等性和多账号合并缺口。
7. 只有所有有效类目 completed_valid 或有明确证据支持的排除原因时，才能宣布完成。
8. 输出“已证明完整、仍有缺口、需要人工复核”。

阶段 9：交付
1. 原始 JSONL 是事实源，必须先完整归档。
2. 如果要求入库，先 dry-run；只存原始门店商品事实和来源标识，不在原始表中做业务比价。
3. 如果要求 Excel，使用指定模板生成业务投影，商品名称不得改写，不暴露账号、Profile、CDP、
   runId、本机路径、接口或请求码。
4. 输出最终批次摘要、原始产物位置、哈希、完整性报告、风险事件和未解决事项。

现在从阶段 0 开始。先只读验证和恢复事实，不要立即打开门店或触发商品请求。
```

## 开始执行后如何回复 Codex

Codex 要求登录时，只回复：

```text
<WORKER>/<SLOT> 已人工登录完成，请先做只读账号和门店预检。
```

Codex 要求处理验证时，只回复：

```text
<WORKER>/<SLOT> 的验证已人工完成，请按恢复 Canary 检查后续采。
```

不要只回复“好了”或“继续”，除非 Codex 上一条消息已经明确列出当前批次、门店、Slot、账号、
Profile 和 checkpoint。
