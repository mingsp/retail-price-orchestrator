# 已验证与待现场验证能力

## 验证层级

- **L0 静态完整：** 文件、清单、SHA-256、脱敏和文档链接。
- **L1 离线可执行：** 样例回放、单元测试、类型检查和构建。
- **L2 本机集成：** Master、Worker、Dashboard 和依赖 E2E。
- **L3 现场 Canary：** 新设备、真实 CDP、真实门店小类目。
- **L4 正式批次：** 全量采集、质量门、导出和入库。

## 历史已验证

2026-07-15 报告记录：

- Master/Worker/Dashboard 单元测试和生产构建通过。
- PostgreSQL fenced lease、Worker 独立凭据和绑定约束。
- MinIO 精确版本、SHA-256、冻结和下载。
- 原始 JSONL、结构化 SPU/SKU 和质量门。
- Redis/Master 故障注入。
- 桌面和移动 Dashboard 验收。

该报告是特定代码和本机 Compose 的历史证据，不证明当前工作树或新设备仍然通过。

## 当前交接发布时重新验证

发布报告必须写入本次实际结果：

- `pnpm handoff:test`
- `pnpm test`
- `pnpm typecheck`
- `pnpm build:production`
- 脱敏样例回放
- 发布包 Manifest/SHA-256
- ZIP 干净解压复验

任何失败原样记录，不使用历史结果替代。

## 仍需现场验证

- 新增 Master 或 Worker 主机的 HTTPS、备份、固定地址和独立 identity。
- 新增设备的可见 Chrome、Browser Slot 和远程桌面。
- 每次正式批次的真实账号/Profile/CDP/门店绑定回传。
- 网络和代理规则。
- 验证码/403/418 风险通知。
- Worker 断网、重启和跨 Worker 迁移。
- 一个真实小类目 Canary。
- 正式门店全量和数据库读回。

## 禁止的完成声明

- “系统显示 100%，所以门店完整。”
- “历史 E2E 通过，所以新设备可用。”
- “接口返回成功，所以数据已落盘。”
- “Excel 有行，所以原始 SKU 完整。”
- “账号能登录，所以目标门店可采。”

完成声明必须带当前时间、环境、命令或业务证据和明确分母。

## 2026-08-17 本机 P0 重新验证

- L1：完整单元测试、类型检查、生产构建和公开仓库脱敏校验通过。
- L2：双 Worker E2E 通过，覆盖账号/Profile/CDP/门店绑定、固定类目、租约 fencing、跨 Worker 迁移、原始 JSONL、Master 质量裁决、不可变对象版本和 Excel 下载。
- 依赖降级：Redis 或 MinIO 单独暂停时 `/ready` 返回 503、控制面查询仍可用；恢复后 `/ready` 回到 200。
- 备份恢复：PostgreSQL 与 MinIO 联合备份及隔离恢复通过，实测 RTO 记录在当次 `restore-drill-result.json`。
- 详细命令、时间和边界见 `docs/operations/2026-08-17-production-closure-report.md`。

## 2026-08-17 远端生产切换验证

- 66 先完成单节点基础设施 Canary，再切换 201；两端没有同时执行首轮切换。
- 66 与 201 的 Master/Dashboard 均运行 `v0.2.5`，`/ready` 和带操作台身份的 `/api/version` 返回 200，版本、commit 和结构版本一致。
- 201 切换前后关键表行数一致，活动任务为 0；切换后 4 个 Worker、10 个 CDP 席位重新连接并持续上报心跳。
- 66 切换后本地 Worker 重新连接；该节点当时没有活动任务和已登记 CDP 席位。
- 两端均保留发布前 PostgreSQL、MinIO、运行配置和旧镜像回滚证据；切换后使用实际备份完成隔离恢复演练并自动清理临时资源。
- 详细时间、备份哈希、回滚镜像和部署中发现的问题见 `docs/operations/2026-08-17-github-66-201-release-handoff.md`。

以上证明基础设施切换和控制面恢复，不替代真实账号、真实门店小类目、验证码通知、正式全量批次或新人独立操作的 L3/L4 验收。
