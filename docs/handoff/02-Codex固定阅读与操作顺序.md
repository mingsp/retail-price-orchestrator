# Codex 固定阅读与操作顺序

## 阶段 1：离线理解

1. 读取 `HANDOFF_START_HERE.md` 和 `AGENTS.md`。
2. 读取 `docs/handoff/00`、`01`、`04`、`05`、`07`、`08`。
3. 运行发布包、脱敏、文档链接和样例回放检查。
4. 输出当前理解，不连接真实环境。

## 阶段 2：现场只读

按顺序核对：

1. Master 进程和依赖。
2. Worker 心跳、版本、时间、磁盘和网络。
3. Browser Slot 与账号/Profile/CDP/门店绑定。
4. 页面标题、`poi_id_str`、登录状态和风险状态。
5. 未完成任务、lease、checkpoint 和 artifact。

输出必须分成：

- 已确认。
- 未确认。
- 与历史文档不一致。
- 需要人工协助。

## 阶段 3：Canary

仅在用户明确批准后：

1. 选择健康 Slot。
2. 确认账号、Profile 和门店。
3. 选择最小类目。
4. 使用批准的低频参数。
5. 观察 checkpoint、raw、progress、summary 和风险事件。
6. 执行数据质量审计。

## 阶段 4：正式批次

- 先创建门店批次和固定类目分配。
- 每个账号只采自己负责的类目。
- 空闲账号只能承接明确迁移的剩余任务。
- 迁移前确认旧任务停止、lease 变化和 checkpoint 可用。
- 采集中只输出精简业务摘要。

## 阶段 5：完成验收

- 对账计划类目、有效类目标记和缺口。
- 对账唯一 SPU、嵌套 SKU 和结构化 SKU。
- 报告门店唯一商品与类目关系两个口径。
- 检查展示价覆盖、商品名、artifact 哈希和重复执行。
- 只有质量门通过的批次才能导出和入库。

## 停止条件

以下情况停止当前 Slot 新请求，但保留会话和断点：

- 验证码或身份核验。
- 403、418 或平台异常码。
- 页面登录失效。
- 门店 ID 不一致。
- 商品区域不可见。
- Worker 心跳丢失。
- 输出目录或任务身份不一致。
